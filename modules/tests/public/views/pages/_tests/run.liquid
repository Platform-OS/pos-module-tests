---
layout: modules/tests/test
---
{% liquid
  if context.environment == 'staging' or context.environment == 'development'
    include 'modules/tests/commands/run', format: context.params.formatter
  endif
%}
