---
layout: test
---
{% liquid
  if context.environment == 'staging' or context.environment == 'development'
    function mails = 'modules/tests/queries/sent_mails/search', limit: 20

    render 'modules/tests/tests/sent_mails_list', mails: mails
  endif
%}
